---

- name: Example playbook with conditional variable
  hosts: localhost
  
  vars:
    my_variable: "{{ 'Value A' if my_condition else 'Value B' }}"

  tasks:
    - name: Show the variable
      vars:
        my_variable: 0
      debug:
        msg: "The variable is set to: {{ my_variable }}"
